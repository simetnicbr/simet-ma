# development config

################################################################################
# Dev config; not supplied on deployment;
################################################################################

# mock service discovery API
MOCK_API_SERVICE_DISCOVERY="true"
MOCK_AUTHORIZATION="true"

################################################################################
# Main config; supplied externally on deployment;
################################################################################

# authentication
AGENT_ID_FILE="./dist/agent-id"
AGENT_TOKEN_FILE="./dist/agent.jwt"

# concurrency control
AGENT_LOCK=/var/lock/simet-agent-unix.lock

# service discovery
API_SERVICE_DISCOVERY="https://api.lab.simet.nic.br/service-list/services?filter%5Bservices%5D=serverMonitor,twamp,collector"

# lmap reporting
TEMPLATE_DIR="./dist"

# capability executables
#TWAMPC=NO
TWAMPC="mock() { echo {} ; return 1; } ; mock "
